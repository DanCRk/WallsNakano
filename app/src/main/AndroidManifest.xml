<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.futurefix.wallsnakano">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_nakanoswpps"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_nakanoswpps"
        android:supportsRtl="true"
        android:theme="@style/Theme.WallsNakano"
        tools:ignore="AllowBackup">
        <activity android:name=".CategoriasWpps" />
        <activity android:name=".ConfigActivity" />
        <activity android:name=".VistaWallpaper" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WallsNakano.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>